RLToolkit namespace structure:

* RLToolkit
  - loggerLayer
  * Basic
    -CfgManager
    -CmdRunner
    -FileWrapper
    -IniParser
    -OSDetector
    -TimerManager
    -XmlHelper
    -MailSystem (not in yet)
    -OBJLoader (not in yet)
  * Wrappers
    -P4Wrapper (not in yet)
    -SVNWrapper (not in yet)

* = namespace 
- = module