RLToolkit namespace structure:

* RLToolkit
  *Logger
    -loggerLayer
  * Basic
    -CfgManager
    -CmdRunner
    -FileWrapper
    -IniParser
    -OSDetector
    -TimerManager
    -XmlHelper
    -MD5Helper
    -StringCrypt
    -TextureHandler
    -MailSystem (not in yet)
    -OBJLoader (not in yet)
  * Extensions
    -BitmapAssert
    -ColorExtension
    -GtkWidgetExtension
  * Widgets
    -ButtonImage
    -DynamicColumn
    -DynamicRow
  * Wrappers
    -P4Wrapper (not in yet)
    -SVNWrapper (not in yet)
  * UnitTests
    * Modules
      -All unit tests for the modules
    * Extensions
      -All unit tests for the extensions
    * Widgets
      -All unit tests for the widgets (see if it's possible to do)
  
* = namespace 
- = module